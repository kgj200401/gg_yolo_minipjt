[project]
name = "smart-cart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "강건재", email = "kgj200401@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.141.1",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.4",
]

[project.scripts]
smart-cart = "smart_cart:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
