# 파인튜닝 진행

## 1. 목표와 현재 상태

**잘못된 상품을 장바구니에 담는 일을 줄이는 정밀도를 우선하면서, 놓치는 상품도 줄인다.**
실험의 목표는 ‘정밀도 90% 이상인 조건 중 Recall이 가장 높은 설정’이다.
90%는 목표 기준이며 실제 카메라에서도 보장되는 수치는 아니다.

- 현재 웹앱 모델: `runs/detect/ozm_augmented/weights/best.pt`
- 자동 담기: 같은 상품이 **신뢰도 70% 이상으로 1초 이상** 확인되면 1개 추가
- 계속 보이는 상품은 중복 등록하지 않고, 사라진 것이 감지된 후 다시 나타나면 재등록
- 신뢰도 55%·60% 평가까지 완료했지만 **앱 설정은 아직 70%**
- B·C 실험 모두 완료했다. 정밀도 우선 목표에서는 A를 유지한다. C 해석은 9절 끝을 참고한다.

## 2. 용어를 쉽게 이해하기

| 용어 | 의미 |
|---|---|
| 파인튜닝 | 이미 학습된 모델을 우리 식재료 데이터에 맞게 추가 학습하는 것 |
| 가중치 | 학습하면서 바뀌는 모델 내부 숫자. 결과는 `best.pt` 등에 저장 |
| 에포크 | 전체 학습 데이터를 한 번 학습하는 단위 |
| 배치 크기 | 한 번에 처리하는 이미지 수 |
| 학습률 | 가중치를 얼마나 크게 수정할지 조절하는 값 |
| 얼리 스토핑 | 검증 성능이 일정 기간 좋아지지 않으면 학습을 멈추는 기능 |
| 신뢰도 기준 | 모델이 낸 결과 중 어느 점수 이상을 사용할지 정하는 값. 변경에 재학습은 필요 없음 |
| 정밀도(Precision) | 탐지한 것 중 정답의 비율. 오탐이 적을수록 높음 |
| 재현율(Recall) | 실제 정답 중 찾아낸 비율. 놓친 물체가 적을수록 높음 |
| F1 | 정밀도와 Recall을 함께 평가하는 조화평균 |
| mAP50 | 박스 IoU 0.5 기준에서 여러 신뢰도 구간을 종합한 클래스 평균 AP |
| mAP50-95 | 박스 IoU 0.5~0.95의 여러 기준으로 더 엄격하게 평가한 평균 AP |
| IoU | 예측 박스와 정답 박스가 겹치는 정도 |

‘정밀도 90%’와 ‘신뢰도 기준 90%’는 다른 의미다.
예를 들어 신뢰도 기준을 60%로 설정해도 평가 정밀도는 90% 이상일 수 있다.

## 3. 데이터와 공통 학습 설정

Roboflow에서 YOLOv8 형식으로 내려받은 데이터셋을 사용했다.
중복 설정 파일과 경로를 정리하고, 다각형 라벨을 객체 탐지용 외접 사각형으로 변환했다.
다운로드한 원본 ZIP은 보존했다.

| 분할 | 이미지 수 | 역할 |
|---|---:|---|
| train | 원본 622장 | 모델 학습 |
| valid | 78장, 정답 박스 216개 | 학습 중 성능 확인, 설정 선택 |
| test | 78장, 정답 박스 219개 | 모델 비교 및 평가 |

증강은 train에만 적용했다. 증강본은 새로 촬영한 독립 사진이 아니므로
비율을 맞추기 위해 valid·test로 옮기지 않았다.
원본 분할 사이에 파일 해시가 완전히 같은 이미지는 없었다.
다만 비슷한 장면이나 연속 촬영 사진의 중복까지 없다고 확인한 것은 아니다.

| 설정 | 값 |
|---|---|
| 시작 가중치 | 기본 `yolov8n.pt` |
| 장비 | RTX 4060 Laptop GPU, 약 8GB VRAM |
| 최대 에포크 | 150 |
| 배치 | 16 |
| 이미지 크기 | 640 |
| 옵티마이저 | AdamW |
| 초기 학습률 | 0.001 |
| 학습률 조절 | 코사인 감소, warmup 3에포크 |
| 얼리 스토핑 | 검증 성능 25에포크 미개선 시 종료 |
| 시드 | 42 |

두 모델 모두 기본 가중치에서 새로 시작했다. 증강 모델은 이전 `best.pt`를 이어받지 않았다.
`best.pt`는 검증 mAP50-95에 기반한 최고 모델이며, 정밀도만으로 선택한 파일은 아니다.

## 4. 기본 학습과 증강 학습

| 항목 | 기본 학습 | 증강 학습 |
|---|---|---|
| 학습 이미지 | 원본 622장 | 원본 622장 + 시계 방향 90도 회전본 622장 |
| 수평 뒤집기 | 확률 50% | 확률 50% |
| 추가 무작위 회전 | 없음 | ±15도 |
| 수평·수직 shear | 없음 | 각각 ±10도 |
| 기본 색상·이동·확대축소·Mosaic | 유지 | 유지 |
| 최고 에포크 | 102 | 121 |
| 종료 에포크 | 127 | 146 |
| 결과 폴더 | `runs/detect/ozm_baseline` | `runs/detect/ozm_augmented` |

수평 뒤집기는 기본 학습에도 적용돼 있었다. 증강 실험에서는 여러 변형을 동시에
추가했으므로 어떤 증강 하나가 성능 변화의 원인인지는 아직 분리해서 확인하지 않았다.

### 동일한 test 분할 비교

Precision·Recall·F1은 신뢰도 **0.70**, 정답 매칭 IoU **0.50**의 전체 물체 합산(micro) 기준이다.
NMS IoU는 0.70이다. mAP는 별도로 낮은 신뢰도 0.001부터 예측을 수집해 계산했다.

| 지표 | 기본 모델 | 증강 모델 | 변화 |
|---|---:|---:|---:|
| **Precision** | 80.87% | **92.81%** | +11.93%p |
| Recall | 67.58% | 58.90% | -8.68%p |
| F1 | 73.63% | 72.07% | -1.56%p |
| mAP50 | 82.44% | 83.53% | +1.09%p |
| mAP50-95 | 63.54% | 63.98% | +0.45%p |
| 정답 탐지 TP | 148 | 129 | -19 |
| 오탐 FP | 35 | 10 | -25 |
| 미탐 FN | 71 | 90 | +19 |

**해석:** 오탐 25개를 줄였지만 정답도 19개 덜 찾았다. 모든 성능이 좋아진 것은 아니다.
마늘은 정답 탐지가 35개에서 18개로 줄어 Recall 하락의 큰 부분을 차지했다.
마늘 Precision은 70%→100%, Recall은 87.5%→45%로 변했다.
표본이 작은 클래스의 100%는 실환경 무오탐을 의미하지 않는다.

검증 최고 mAP50-95도 기본 모델 72.87%, 증강 모델 66.12%였다.
학습을 길게 하거나 증강을 늘리면 무조건 좋아진다고 판단할 근거는 없다.

## 5. 첫 튜닝 실험: 신뢰도 기준 비교 — 완료

### 방법

- 현재 증강 모델을 그대로 사용하고 **재학습하지 않았다**.
- valid 78장으로 0.50~0.90을 0.05 간격으로 비교했다.
- test는 이번 실험에 사용하지 않았다.
- 정밀도 90% 이상인 조건 중 Recall이 가장 높은 조건을 찾았다.
- 정답 매칭 IoU 0.50, NMS IoU 0.70, 입력 640을 유지했다.

| 신뢰도 기준 | Precision | Recall | F1 | TP | FP | FN |
|---|---:|---:|---:|---:|---:|---:|
| 50% | 86.90% | 67.59% | 76.04% | 146 | 22 | 70 |
| **55%** | **90.32%** | **64.81%** | **75.47%** | 140 | 15 | 76 |
| **60%** | **90.32%** | **64.81%** | **75.47%** | 140 | 15 | 76 |
| 65% | 91.72% | 61.57% | 73.68% | 133 | 12 | 83 |
| 현재 70% | 91.67% | 56.02% | 69.54% | 121 | 11 | 95 |
| 75% | 92.56% | 51.85% | 66.47% | 112 | 9 | 104 |
| 80% | 93.75% | 48.61% | 64.02% | 105 | 7 | 111 |
| 85% | 94.57% | 40.28% | 56.49% | 87 | 5 | 129 |
| 90% | 94.44% | 23.61% | 37.78% | 51 | 3 | 165 |

이번 검증의 mAP50은 83.65%, mAP50-95는 66.10%다.
mAP는 신뢰도 한 지점의 정밀도와 다른 지표이므로 각 행마다 다시 성능 향상 수치로 해석하지 않는다.

### 판단

55%와 60%가 이 검증 표본에서 같은 결과를 냈다. 평가 스크립트는 동률의 첫 값인 55%를
선택했지만, 실제 카메라 시험 후보로는 그중 높은 기준인 **60%**를 제안한다.
60%가 다른 데이터에서도 더 좋다고 입증된 것은 아니다.

70%→60% 변경 시 검증 결과:

- 정밀도 **1.35%p 감소**, Recall **8.80%p 증가**, F1 **5.93%p 증가**
- 정답 19개를 더 찾는 대신 오탐 4개 추가
- 목표 정밀도 90%는 근소하게 넘었으므로 새 영상에서도 확인 필요

**주의:** test 정밀도 92.81%와 valid 정밀도 91.67%는 서로 다른 데이터의 결과다.
조건 변경 효과는 같은 분할 내에서 비교해야 한다.

## 6. 제안했던 단계와 변경 사항

사용자와 논의 후 카메라 시험은 보류하고 B 실험부터 진행하기로 했다.
아래 50에포크 계획은 초기 제안이다. 기존 모델과 같은 학습 정책으로 비교하기 위해
실제 B 실험은 최대 150에포크·patience 25로 변경했다. C는 아직 실행하지 않는다.

### 먼저: 신뢰도 60%·1초로 실제 카메라 비교

새 학습보다 먼저, 현재 70% 설정과 후보 60% 설정을 같은 영상에서 비교한다.
가능하면 영상을 한 번 녹화해 두 조건에 똑같이 재생하고, 새 장면·조명·거리도 포함한다.
상품별 10회 이상 등장시키는 것을 초기 점검 목표로 삼되 최종 통계 검증으로 과대해석하지 않는다.
빈 화면과 손만 나오는 장면, 상품을 치웠다가 다시 넣는 장면도 포함한다.

| 측정 항목 | 의미 |
|---|---|
| 등록 정밀도 | 자동 등록 중 올바른 상품 등록 비율 |
| 등록 성공률 | 실제 상품 제시 중 올바르게 등록된 비율 |
| 중복 등록 | 상품을 계속 보여줬는데 추가로 담긴 횟수 |
| 등록 지연 | 상품 제시부터 등록까지 시간 |

정지 이미지 평가에는 1초 유지·재등장 조건이 포함되지 않았으므로 앱 성능과 구분한다.
60%에서 오등록이 허용 범위 안이고 누락이 줄면 재학습을 보류할 수 있다.

### 필요하면: 짧은 재학습 B → 결과 검토 → C

| 실험 | 변경 | 학습 한도 |
|---|---|---|
| B | 90도 회전본 제외, 회전 ±15도·shear ±10도 유지 | 최대 50에포크, patience 15 |
| C | B와 같은 데이터, 회전 ±10도·shear ±3도로 완화 | 최대 50에포크, patience 15 |

B 결과를 먼저 보고 C 여부를 결정한다. 배치 16, AdamW, lr0=0.001 등 나머지 조건을 맞춘다.
기존 증강 모델의 50에포크 기록은 데이터 수와 학습률 일정이 달라 참고용이다.
짧은 실험이 유망할 때만 후보 하나를 길게 학습한다.
이후 검증에서 임계값을 선택하고, 최종 확인은 추가 독립 데이터에서도 수행한다.
기존 test를 이미 여러 차례 비교했으므로 계속 그 점수에 맞춰 튜닝하지 않는다.

## 7. 실행 파일과 결과 위치

| 파일 | 역할 |
|---|---|
| `train_model.py` | 학습 파라미터 설정 및 학습 |
| `prepare_augmentation.py` | train 90도 회전본과 라벨 생성 |
| `run_augmented_training.py` | 증강 학습·test 평가·비교표 생성 |
| `evaluate_model.py` | 모델별 test 평가 |
| `evaluate_thresholds.py` | 이번 valid 신뢰도 비교 |
| `runs/evaluation/report.json` | 기본 모델 test 수치 |
| `runs/evaluation_augmented/report.json` | 증강 모델 test 수치 |
| `runs/threshold_validation/report.json` | 이번 valid 신뢰도별·클래스별 수치 |
| `frontend/src/detectionTracker.js` | 앱 자동 등록 신뢰도·시간 조건 |

`runs/`는 Git 제외 대상이다. 공유할 주요 수치는 이 문서에 함께 기록한다.
후속 실험마다 설정·데이터 분할·수치·판단·실제 적용 여부를 이 문서에 이어서 갱신한다.

## 8. B 실험: 90도 회전본 제외

### 개선될 수 있다고 생각하는 이유 — 아직 검증되지 않은 가설

1. **실제 카메라와의 모습 차이를 줄일 가능성:** 기존 증강 데이터 절반은 90도 회전본이다.
   실제 사용에서 그런 방향이 드물다면, 제외 시 실제로 자주 보이는 모습에 학습을 집중할 수 있다.
   단, 실제 방향 분포는 아직 측정하지 않았고 식재료를 돌려 보여주는 환경에서는 도움이 될 수도 있다.
2. **변형이 겹치는 부담을 줄일 가능성:** 90도 회전본에도 ±15도 회전·±10도 shear와
   기본 Mosaic·확대축소 등이 적용된다. 불필요하게 어려운 변형을 줄이면 원래 장면의 탐지에
   도움이 될 수 있다. 다만 90도 회전 자체는 사각형 라벨을 정확히 변환할 수 있는 증강이다.
3. **Recall 저하 원인을 분리해서 확인:** 증강 후 test 오탐은 35→10개로 감소했지만
   정답 탐지도 148→129개로 감소했다. 여러 증강을 동시에 추가했으므로 회전본의 영향은 아직 모른다.
   회전본만 제외하고 다른 설정을 유지해 정밀도·Recall 변화가 있는지 확인한다.

이 가설이 맞으면 정밀도를 크게 잃지 않으면서 Recall이 개선될 수 있다.
반대로 다양한 방향을 덜 학습해 성능이 떨어질 수도 있다. 결과를 보고 판단한다.

### 실제 실행 조건

| 항목 | A: 기존 증강 모델 | B: 이번 실험 |
|---|---|---|
| 시작 모델 | 기본 yolov8n.pt | 동일, 이전 best.pt 이어 학습하지 않음 |
| 학습 데이터 | 원본 622 + 90도 회전본 622 | 원본 622만 |
| 무작위 회전 | ±15도 | 동일 |
| 수평·수직 shear | 각각 ±10도 | 동일 |
| 수평 뒤집기 | 확률 50% | 동일 |
| 기본 Mosaic·색상·크기·이동 증강 | 유지 | 동일 |
| 최대 에포크 / patience | 150 / 25 | 동일 |
| 배치 / 이미지 크기 | 16 / 640 | 동일 |
| 최적화 / 초기 학습률 | AdamW / 0.001 | 동일 |
| 코사인 감소 / 시드 | 적용 / 42 | 동일 |
| valid / test 분할 | 기존 분할 | 동일 |

**비교 한계:** 같은 에포크여도 A는 이미지 수가 2배여서 배치 처리량과 계산량이 다르다.
이번 실험은 같은 학습 정책에서 회전본 포함·제외의 실용적 효과를 비교하며,
회전 변환 자체의 효과만 계산량까지 통제한 실험은 아니다. 시드 한 번의 결과로 일반화하지 않는다.

### 평가와 적용 기준

- 학습 종료 후 best.pt를 valid 78장에 평가한다. test는 이번 단계에서 재평가하지 않는다.
- A의 기존 valid 신뢰도 비교와 B를 **같은 70% 기준**으로 먼저 비교한다.
- 50~90% 신뢰도별 수치와 ‘정밀도 90% 이상 중 최고 Recall’ 조건도 별도로 비교한다.
- Precision·Recall·F1, TP·FP·FN, mAP50·mAP50-95, 클래스별 결과와 학습 시간을 기록한다.
- 서로 다른 신뢰도에서 비교하는 수치는 각 모델의 설정 선택 결과이며 같은 조건 비교와 구분한다.
- 성능이 좋다고 자동 적용하지 않는다. 결과를 검토하고 다음 실험이나 앱 적용을 결정한다.

실행 파일: `run_no_rotation_experiment.py`

로그: `runs/no_rotation_training.log`

결과 모델: `runs/detect/ozm_no_rotation/weights/best.pt`

평가: `runs/validation_no_rotation/report.json`

<!-- B_RESULT_START -->
### B 실험 결과 — 학습 및 평가 완료

시작: 2026-09-07T18:45:44 / 완료: 2026-09-07T19:19:15
실제 학습: 150에포크 / 최고 검증 에포크: 131
학습 CSV 기준 경과 시간: 33.1분

**같은 valid 78장, 신뢰도 70%에서 비교** (mAP는 별도로 전체 신뢰도 구간 평가)

| 지표 | A: 90도 포함 | B: 90도 제외 | 변화(%p) |
|---|---:|---:|---:|
| precision | 91.67% | 89.71% | -1.96 |
| recall | 56.02% | 56.48% | +0.46 |
| f1 | 69.54% | 69.32% | -0.22 |
| map50 | 83.65% | 85.80% | +2.15 |
| map50_95 | 66.10% | 64.69% | -1.42 |

TP/FP/FN: A 121/11/95 → B 122/14/94

**각 모델에서 정밀도 90% 이상 중 최고 Recall 조건**

| 모델 | 신뢰도 | Precision | Recall | F1 |
|---|---:|---:|---:|---:|
| A | 55.00% | 90.32% | 64.81% | 75.47% |
| B | 75.00% | 90.55% | 53.24% | 67.06% |

**B 신뢰도별 수치**

| 신뢰도 | Precision | Recall | F1 |
|---|---:|---:|---:|
| 50.00% | 83.53% | 65.74% | 73.58% |
| 55.00% | 83.93% | 65.28% | 73.44% |
| 60.00% | 85.80% | 64.35% | 73.54% |
| 65.00% | 87.42% | 61.11% | 71.93% |
| 70.00% | 89.71% | 56.48% | 69.32% |
| 75.00% | 90.55% | 53.24% | 67.06% |
| 80.00% | 92.79% | 47.69% | 63.00% |
| 85.00% | 93.20% | 44.44% | 60.19% |
| 90.00% | 100.00% | 26.85% | 42.34% |

**클래스별 70% 기준 비교**

| 클래스 | A Precision | B Precision | A Recall | B Recall |
|---|---:|---:|---:|---:|
| apple | 87.50% | 81.25% | 100.00% | 92.86% |
| bread | 100.00% | 93.33% | 82.35% | 82.35% |
| carrot | 100.00% | 해당 없음 | 25.00% | 0.00% |
| egg | 83.33% | 77.78% | 62.50% | 87.50% |
| galic | 77.78% | 77.78% | 63.64% | 63.64% |
| l_onion | 100.00% | 100.00% | 50.00% | 33.33% |
| onion | 100.00% | 97.44% | 62.07% | 65.52% |
| raw_pork | 87.50% | 85.71% | 35.00% | 30.00% |
| shrimp | 93.75% | 100.00% | 46.88% | 56.25% |
| sliced_ham | 71.43% | 71.43% | 83.33% | 83.33% |

test 재평가 및 앱 자동 적용은 하지 않았다. 검증 표본이 작으므로 개선 여부를 과대해석하지 않는다.
<!-- B_RESULT_END -->

### B 결과 해석 및 다음 판단

- 150에포크까지 완료했고 최고 모델은 131에포크다. 학습 시간은 약 33.1분이다.
- 신뢰도 70%에서 정답 탐지는 1개 증가했지만 오탐은 3개 늘었다.
  정밀도 91.67%→89.71%, Recall 56.02%→56.48%로 목표에 유리한 개선은 확인되지 않았다.
- 정밀도 90%를 만족하는 후보 중 A는 신뢰도 55% 또는 60%에서 Recall 64.81%,
  B는 75%에서 Recall 53.24%다. 이번 검증에서는 A가 정밀도·Recall 균형에 더 유리하다.
- B의 mAP50은 개선됐으나 mAP50-95는 낮아졌다. 박스 위치를 엄격하게 평가한 성능까지
  함께 좋아진 것은 아니다.
- ‘90도 회전본을 제외하면 더 좋아질 수 있다’는 가설을 이번 결과는 뒷받침하지 않는다.
  단일 시드, 작은 검증 표본, 다른 데이터 처리량 때문에 회전 증강의 보편적 효과로 단정하지 않는다.
- B는 앱에 적용하지 않고 기존 A 모델을 유지한다. 다음 실험은 자동 시작하지 않는다.
- 후속으로 회전·shear 강도 완화를 검토할 수 있다. 90도 포함 A에서 완화하면 A와,
  90도 제외 B에서 완화하면 B와 비교해야 한다. 비교 대상을 정한 뒤 실행한다.

## 9. C 실험: 90도 회전본 유지, shear만 완화

### 변경 이유와 가설

B에서 90도 회전본을 제외해도 정밀도 우선 목표의 개선은 확인되지 않았다.
이번에는 기준을 기존 증강 모델 A로 되돌려 **수평·수직 shear만 ±10도에서 ±3도로 낮춘다.**
Shear는 물체를 비스듬히 기울여 모양을 변형한다. 회전·Mosaic·확대축소와 겹칠 때
실제 카메라보다 변형이 강해질 수 있다. 이를 줄이면 상품의 원래 형태를 더 잘 학습하고
정밀도를 유지하면서 미탐을 줄일 수 있다는 가설이다. 아직 입증된 원인은 아니다.
반대로 다양한 시점에 대한 적응력이 줄어 성능이 떨어질 수도 있다.

### 고정 조건과 변경 조건

| 항목 | A: 기존 증강 | C: 이번 실험 |
|---|---|---|
| 데이터 | 원본 622장 + 90도 회전본 622장 | 동일한 1,244장 |
| 수평·수직 shear | 각각 ±10도 | **각각 ±3도** |
| 무작위 회전 / 수평 뒤집기 | ±15도 / 50% | 동일 |
| 최대 에포크 / patience | 150 / 25 | 동일 |
| 배치 / 이미지 크기 | 16 / 640 | 동일 |
| 최적화 / 초기 학습률 | AdamW / 0.001 | 동일 |
| 코사인 학습률 / 시드 | 적용 / 42 | 동일 |
| 시작 가중치 | 기본 yolov8n.pt | 동일, A·B 가중치를 이어받지 않음 |
| 기본 Mosaic·색상·이동·크기 증강 | 유지 | 동일 |

이전의 ‘90도 제외 + 회전·shear 동시 완화’ C 제안은 취소하고, 위 단일 변수 실험으로 변경했다.
A와 C는 데이터 수와 학습 정책도 동일하다. 실제 종료 에포크는 조기 종료에 따라 달라질 수 있다.

### 비교 방법과 다음 결정

- 같은 valid 78장에서 신뢰도 70%의 Precision·Recall·F1, TP·FP·FN과 mAP를 비교한다.
- 50~90% 임계값 평가도 수행해 정밀도 90% 이상 중 최고 Recall을 비교한다.
  A의 기준은 Precision 90.32%, Recall 64.81%다(신뢰도 55%와 60%에서 동률).
- 클래스별 수치, 최고·종료 에포크, 학습 시간도 기록한다. test는 이번에 다시 평가하지 않는다.
- 이번 설정 실험 후 개선이 없으면 추가 파라미터 학습을 자동 실행하지 않는다.
  미탐·오탐 이미지, 라벨 누락·박스 품질, 실제 카메라 데이터 보강을 우선 검토한다.
- 현재 앱의 A 모델 및 70%·1초 조건은 유지한다. 결과에 따른 적용은 별도로 결정한다.

실행 파일: `run_shear_experiment.py`

로그: `runs/shear3_training.log`

모델: `runs/detect/ozm_shear3/weights/best.pt`

평가 및 비교표: `runs/validation_shear3/report.json`, `runs/validation_shear3/comparison.md`

<!-- C_RESULT_START -->
### C 실험 결과 — 학습 및 평가 완료

시작: 2026-09-07T19:33:53 / 완료: 2026-09-07T20:24:59
실제 학습: 117에포크 / 최고 검증 에포크: 92
학습 CSV 기준 경과 시간: 50.6분

**같은 valid 78장, 신뢰도 70%에서 비교** (mAP는 별도로 전체 신뢰도 구간 평가)

| 지표 | A: shear 10 | C: shear 3 | 변화(%p) |
|---|---:|---:|---:|
| precision | 91.67% | 87.41% | -4.25 |
| recall | 56.02% | 57.87% | +1.85 |
| f1 | 69.54% | 69.64% | +0.10 |
| map50 | 83.65% | 83.46% | -0.19 |
| map50_95 | 66.10% | 69.00% | +2.90 |

TP/FP/FN: A 121/11/95 → C 125/18/91

**각 모델에서 정밀도 90% 이상 중 최고 Recall 조건**

| 모델 | 신뢰도 | Precision | Recall | F1 |
|---|---:|---:|---:|---:|
| A | 55.00% | 90.32% | 64.81% | 75.47% |
| C | 80.00% | 90.83% | 45.83% | 60.92% |

**C 신뢰도별 수치**

| 신뢰도 | Precision | Recall | F1 |
|---|---:|---:|---:|
| 50.00% | 76.80% | 64.35% | 70.03% |
| 55.00% | 77.46% | 62.04% | 68.89% |
| 60.00% | 82.28% | 60.19% | 69.52% |
| 65.00% | 84.87% | 59.72% | 70.11% |
| 70.00% | 87.41% | 57.87% | 69.64% |
| 75.00% | 89.76% | 52.78% | 66.47% |
| 80.00% | 90.83% | 45.83% | 60.92% |
| 85.00% | 95.45% | 38.89% | 55.26% |
| 90.00% | 100.00% | 21.76% | 35.74% |

**클래스별 70% 기준 비교**

| 클래스 | A Precision | C Precision | A Recall | C Recall |
|---|---:|---:|---:|---:|
| apple | 87.50% | 81.25% | 100.00% | 92.86% |
| bread | 100.00% | 93.33% | 82.35% | 82.35% |
| carrot | 100.00% | 100.00% | 25.00% | 50.00% |
| egg | 83.33% | 75.00% | 62.50% | 75.00% |
| galic | 77.78% | 77.78% | 63.64% | 63.64% |
| l_onion | 100.00% | 100.00% | 50.00% | 50.00% |
| onion | 100.00% | 88.37% | 62.07% | 65.52% |
| raw_pork | 87.50% | 94.74% | 35.00% | 30.00% |
| shrimp | 93.75% | 100.00% | 46.88% | 59.38% |
| sliced_ham | 71.43% | 55.56% | 83.33% | 83.33% |

test 재평가 및 앱 자동 적용은 하지 않았다. 검증 표본이 작으므로 개선 여부를 과대해석하지 않는다.
<!-- C_RESULT_END -->

### C 결과 해석과 최종 방향

**결론: shear를 ±3도로 낮추면 박스 위치 평가 일부는 개선됐지만, 정밀도 우선 목표에서는 A보다 불리했다.**

- 최고 모델은 92에포크, 종료는 117에포크다. 학습 시간은 약 50.6분이다.
- 같은 신뢰도 70%에서 TP는 121→125개(+4), FP는 11→18개(+7)다.
  정밀도는 4.25%p 감소했고 Recall은 1.85%p 증가했다. F1 변화 +0.10%p는 매우 작다.
- mAP50-95는 66.10%→69.00%로 개선됐다. 이는 더 엄격한 박스 매칭 기준의 평균 AP가
  좋아졌다는 뜻이지, 장바구니 오등록이 줄었다는 뜻은 아니다.
- 정밀도 90% 이상을 충족하는 평가 후보 중 A는 Recall 64.81%, C는 45.83%다.
  해당 목표에서 C가 유리하다는 근거는 없다. 이는 0.05 간격으로 평가한 후보 내 비교다.
- 양파 정밀도는 100%→88.37%, 햄은 71.43%→55.56%로 낮아졌다.
  새우는 정밀도 93.75%→100%, Recall 46.88%→59.38%로 개선됐다.
  일부 클래스가 좋아져도 전체 서비스 목표에 유리한지는 따로 판단해야 한다.
- 작은 검증 표본·단일 시드 결과다. 예를 들어 당근 Recall 25%→50%는 정답 4개 중
  1개 대신 2개를 찾았다는 뜻이므로 큰 일반적 개선으로 해석하지 않는다.

| 검증 조건 | A: 기존 증강 | B: 90도 제외 | C: shear 완화 |
|---|---:|---:|---:|
| 70%에서 Precision | 91.67% | 89.71% | 87.41% |
| 70%에서 Recall | 56.02% | 56.48% | 57.87% |
| Precision 90% 이상 중 최고 Recall | 64.81% | 53.24% | 45.83% |

이번까지 설정 실험을 하기로 했으므로 추가 학습은 시작하지 않는다.
현재 앱의 A 모델 및 70%·1초 조건을 유지하며, 60% 후보도 아직 적용하지 않는다.

다음 단계는 A가 놓친 마늘·대파·고기·새우 등과 오탐 이미지를 직접 살펴보는 것이다.
라벨 누락·박스 범위·낱개와 묶음의 기준, 물체 크기·가림·조명·배경을 구분해서 기록한다.
문제를 확인한 뒤 필요한 실제 카메라 사진과 빈 배경 이미지를 보강한다.
기존 검증·테스트를 점수에 유리하게 고치거나 재분할하지 않고, 수정이 필요한 라벨은
변경 이력을 남기며 모든 비교 모델을 같은 정답으로 재평가한다.
최종 확인에는 새로 촬영한 독립 데이터 및 장바구니 등록 시나리오를 사용한다.

## 10. 데이터 점검 1차 — 라벨 기준 확인 우선

### 진행한 작업

기존 A 모델로 valid 78장을 추론하고 정답·예측 박스를 비교했다.
오탐·미탐 점수가 높은 상위 12장을 직접 시각 검토했다. 원본 이미지·라벨과 앱 설정은 수정하지 않았다.
`audit_dataset.py`로 재현할 수 있다.

- 비교 이미지: `runs/data_audit/errors_1.png` ~ `errors_3.png`
- 전체 이미지별 기록: `runs/data_audit/audit.json`
- 검토할 파일 목록: `runs/data_audit/review_queue.csv`
- 그림 왼쪽 초록색: 정답 라벨 / 오른쪽 빨간색: 신뢰도 70% 이상 예측
- 오른쪽 주황색: 신뢰도 25% 이상, 70% 미만 예측
- 색상 자체가 정답·오답 판정은 아니다. 상단 TP/FP/FN은 클래스 및 IoU 0.5 매칭 기준이다.

### 실제로 발견한 사례

| 우선순위 | 그림 번호·파일 앞부분 | 확인된 모습 | 판단 및 조치 후보 |
|---|---|---|---|
| 1 | #3 `galic17_webp` | 마늘 두 봉지가 보이는데 정답은 마늘 일부의 매우 작은 박스 하나. 모델은 두 봉지를 각각 고신뢰도로 탐지 | 라벨 범위·누락 의심이 강함. 통마늘 1개인지 판매 봉지 1개인지 먼저 기준 확인 |
| 2 | #12 `ham_020_jpg` | 두 팩에 정답 박스 하나, 모델은 두 팩을 각각 예측 | 낱개·묶음의 단위 불일치. 상품 등록 단위에 맞춰 기준 확정 |
| 3 | #9 `apple_055_jpg` | 여러 사과가 보이지만 정답 박스가 없는 부분도 있음 | 가림·화면 경계 물체의 라벨 정책 확인. 누락이면 맞게 찾은 예측도 오탐이 될 수 있음 |
| 4 | #7 `pig_thin sliced pork_009` | 접시의 여러 고기 조각 중 일부에 정답 박스가 있음 | 조각·접시·판매 팩 중 무엇을 하나로 볼지 확인. 부분 라벨링 가능성 점검 |
| 5 | #1 고기, #2·#8 새우 | 물체가 많이 겹쳐 있고 일부 고기는 새우로 혼동 | 실제 분류·가림 문제도 존재. 라벨 문제만으로 전체 오류를 설명할 수 없음 |
| 6 | #6 `onion_042_jpg` | 멀리서 촬영한 매장, 사람과 양파 더미·묶음이 섞임 | 가까운 상품 제시 환경과 데이터 차이. 학습 목적에 맞는 촬영 데이터 보강 검토 |

### 임계값으로 제외되는 사례

이 진단 실행에서는 TP 119, FP 12, FN 97이었다.
FN 97개 중 46개에 같은 클래스·IoU 0.5 이상인 예측 박스가 존재했지만 신뢰도가 70% 미만이었다.
이는 낮은 신뢰도가 상당수 미탐에 관여함을 보여준다. 다만 한 예측이 여러 정답과 겹칠 수 있어
임계값을 내리면 정확히 46개가 복구된다는 뜻은 아니다.

**수치 구분:** 이 진단은 `predict()` 경로의 원본 이미지로 실행했다.
이전 공식 비교는 `val()` 경로이므로 전처리·정밀도 등 실행 차이로 경계 판정이 달라질 수 있다.
진단 TP/FP/FN을 기존 검증 성능 표에 덮어쓰거나 모델 성능 변화로 해석하지 않는다.

### 가장 먼저 할 일

**학습 횟수보다 ‘정답에서 상품 1개가 무엇인지’를 먼저 확정한다.**
현재 앱은 같은 클래스를 묶고 1개씩 등록하므로, 개별 식재료 조각을 모두 찾는 평가와
판매 단위 등록 목적 사이에 차이가 있다. 특히 마늘 봉지·햄 묶음·고기 팩이 중요하다.

1. 상품별로 낱개/봉지/팩/묶음의 박스 기준과 가림·경계 물체의 라벨 규칙을 작성한다.
2. 위 우선순위 사례의 정답을 사람이 확인한다. 특히 작은 마늘 박스와 햄 묶음부터 확인한다.
3. 확정한 기준으로 train에도 같은 문제가 있는지 점검하고 수정 목록·변경 이력을 남긴다.
4. valid 정답을 수정했다면 모든 비교 모델을 같은 수정본으로 재평가한다.
   정답을 바꾼 전후의 점수 차이는 모델 자체 개선이라고 보고하지 않는다.
5. 그 후 실제 카메라에서 누락이 많은 상황을 추가 수집하고 재학습 여부를 정한다.

사진에 보인다고 임의로 라벨을 추가하거나 기존 정답을 자동 변경하지 않았다.
이번 점검만으로 데이터 전체 오류 비율이나 새로운 성능을 주장하지 않는다.

## 11. 데이터 점검 2차 — train 전체 점검과 수정 준비

다음 단계로 **train 622장 전체를 A 모델로 추론하고 오류 후보 상위 12장을 직접 확인**했다.
재학습보다 먼저 학습 정답의 누락과 상품 단위 혼재 여부를 확인하는 작업이다.

| 항목 | 결과 |
|---|---:|
| 추론 점검한 train 이미지 | 622장 |
| FP 또는 FN이 있는 검토 후보 | 131장 |
| 진단 TP / FP / FN | 1,457 / 73 / 233 |
| FN 중 같은 클래스·IoU ≥ 0.5 예측이 있으나 신뢰도 < 70%인 경우 | 201개 |
| train 완전 중복 라벨 | 2행 |
| valid 박스 경계의 미세한 초과 | 1행 |

이 수치는 **학습에 사용했던 이미지의 진단 결과**이며 검증·테스트 성능과 비교하지 않는다.
후보 131장이 모두 라벨 오류라는 뜻도 아니다. 낮은 신뢰도, 가림, 모델 오탐도 포함한다.
미탐 201개 역시 임계값을 낮추면 전부 복구된다는 의미는 아니다.

계란 포장과 사과의 라벨 누락 의심, 대파 잎·줄기의 박스 분리 및 묶음 기준 문제가 보였다.
따라서 판매 단위와 박스 범위를 정리한 [라벨 수정 기준 초안](라벨%20수정%20기준.md)을 작성했다.
완전 중복 2행은 기존 학습에서도 로더가 제거했으므로 이것만으로 성능 개선을 기대하지 않는다.

- 비교 그림: `runs/data_audit_train/errors_1.png` ~ `errors_3.png`
- 전체 진단: `runs/data_audit_train/audit.json`
- 131장 검토 목록: `runs/data_audit_train/review_queue.csv`
- 재현 명령: `.\.venv-train\Scripts\python.exe audit_dataset.py --split train`

**현재 완료:** train 진단, 상위 사례 시각 검토, 수정 기준 초안과 검토 목록 작성.
**다음 단계:** 상품 단위 확정 → 별도 데이터셋 라벨 수정 → 증강본 재생성 → 같은 평가 기준으로 비교.
상품 단위에 대한 답변을 기다리는 중이며, 원본 라벨 변경과 재학습은 아직 실행하지 않았다.
서비스는 기존 A 모델과 신뢰도 70%·1초 조건을 유지한다.

## 12. 판매 단위 라벨 1차 수정 및 A 모델 재평가

### 무엇을 바꿨나

사용자가 판매 단위 기준을 승인하여 **봉지·팩·묶음은 하나, 포장 없는 상품은 낱개**로 정했다.
추가로 판매 단위 후보 30장의 정답 박스를 확인했다. 28장은 그대로 유지하고 명확한 오류 2장을 수정했다.

| 구분 | 변경 |
|---|---|
| train 대파 `large green onion_091` | 잎·줄기로 나뉜 10개 박스를 실제로 묶여 있는 두 단의 박스 2개로 수정 |
| valid 마늘 `galic17` | 마늘 일부만 표시한 작은 박스 1개를 두 봉지의 박스 2개로 수정 |
| train 당근·새우 | 동일 좌표·클래스의 중복 라벨 각 1행 제거 |
| valid 햄 `ham_085` | 미세하게 음수였던 박스 상단 좌표를 이미지 경계로 보정 |

`ham_020`은 `120g × 2 PACK` 판매 포장이므로 박스 하나를 유지했다.
수정은 `dataset_corrected_v1`에만 적용했다. train 622장·valid 78장·test 78장 분할과 이미지 바이트를 보존했다.
원본의 이미지·라벨 SHA256을 수정 전후 비교해 원본이 그대로인지 확인했다.
변경 전후 라벨과 이유는 `dataset_corrected_v1/changes.json`, 그림은 `runs/sale_unit_review/corrected_*.png`에 있다.

### 같은 A 모델을 수정된 정답으로 다시 평가한 결과

**아직 재학습한 결과가 아니다.** 같은 가중치로 정답 라벨만 바꿔 재평가했다.
신뢰도 70%, 정답 매칭 IoU 0.5, NMS IoU 0.7 조건이다. Precision·Recall·F1은 전체 객체 기준 micro 평균이다.
mAP는 별도로 낮은 신뢰도부터 평가한 결과다.

| 지표 | 기존 valid 정답 | 1차 수정 valid 정답 |
|---|---:|---:|
| Precision | 91.67% | 93.18% |
| Recall | 56.02% | 56.68% |
| F1 | 69.54% | 70.49% |
| mAP50 | 83.65% | 85.75% |
| mAP50-95 | 66.10% | 67.44% |
| TP / FP / FN | 121 / 11 / 95 | 123 / 9 / 94 |
| 정답 객체 수 | 216 | 217 |

마늘 두 봉지를 찾은 예측이 오탐에서 정탐으로 바뀌었다.
**이 상승은 모델 개선이 아니라 잘못된 평가 정답을 바로잡은 효과**다.
향후 수정 데이터로 학습한 모델은 오른쪽 기준의 A 모델과 비교해야 한다.
기존 B·C와 비교하려면 그 모델도 동일한 수정 valid로 재평가해야 한다.

수정 valid에서 신뢰도 60% 결과는 Precision **91.61%**, Recall **65.44%**, F1 **76.34%**다.
이는 정적인 이미지 평가이며, 1초 유지 후 장바구니 등록 성공률을 의미하지 않는다.
서비스의 모델과 70%·1초 설정은 유지했다.

### 현재 단계와 다음 판단

1차 수정본과 재평가 결과를 저장했다. 전체 데이터 재라벨링이 완료된 상태는 아니다.
실질적인 train 정답 단위 변경은 대파 1장이므로, 이 상태에서 긴 재학습을 해도 큰 개선을 기대할 근거가 약하다.
우선 남은 밀집 장면의 라벨 누락과 묶음 경계를 더 정리한 뒤, 충분한 수정량이 모이면 A와 같은 설정으로 학습한다.
이번에는 추가 학습을 시작하지 않았으며, 기존 모델을 학습이 끝난 새 모델로 교체하지 않았다.

재현 명령:

```powershell
.\.venv-train\Scripts\python.exe prepare_corrected_dataset.py
.\.venv-train\Scripts\python.exe evaluate_thresholds.py --data dataset_corrected_v1/data.yaml --output runs/threshold_corrected_labels_v1
```

수정본 생성 명령은 기존 출력 폴더가 있으면 덮어쓰지 않고 중단한다.
평가 결과: `runs/threshold_corrected_labels_v1/report.json`, `comparison.md`.

수정 라벨로 90도 회전본 622장도 새로 생성했다.
`dataset_corrected_augmented_v1/data.yaml`은 수정 원본 train 622장과 새 회전본 622장만 참조한다.
기존 증강본의 오래된 라벨이 섞이지 않도록 했고, 622쌍 모두 회전 좌표 변환을 검증했다.
valid는 수정본 78장, test는 원본과 바이트가 같은 78장이다.

```powershell
.\.venv-train\Scripts\python.exe prepare_augmentation.py --source dataset_corrected_v1 --output dataset_corrected_augmented_v1
```

## 13. 데이터 정리 2차 — 누락된 판매 포장과 사과 라벨 보완

### 추가 정리가 필요했던 부분

여기서 정리는 사진 수를 늘리거나 학습 설정을 바꾸는 작업이 아니다.
**사진에 실제로 보이는 상품과 정답 박스를 맞추는 작업**이다.

1. **누락:** 상품이 있는데 정답 박스가 없으면, 학습에서 그 부분이 배경으로 취급될 수 있다.
2. **범위·단위:** 계란 한 포장의 일부만 표시하거나 대파 한 단을 잎·줄기로 나누면 상품 하나의 기준이 흐려진다.
3. **중복:** 같은 상품에 동일한 박스가 겹쳐 있으면 중복 정답을 정리한다. 완전 중복은 1차에서 처리했다.

이번에는 1차에서 남겼던 계란·사과·대파 후보 5장을 검토했다.
정답이 없는 물체를 모델 예측으로 자동 채우지 않고, 원본을 보고 경계를 직접 지정했다.
계란 사진의 중간·아래 선반은 확대해서 포장 경계를 확인했다.

### 실제 수정한 두 사진

| train 사진 | 이전 | 수정 후 | 수정 이유 |
|---|---:|---:|---|
| `31f979235712cba7802420ec7a19b6f2...` 계란 진열대 | 박스 20개 | 박스 43개 | 누락된 판매 포장 추가, 한 포장의 일부만 표시한 범위 수정. 포장 안 계란은 따로 세지 않음 |
| `apple_006...` 사과 | 박스 20개 | 박스 32개 | 기존 20개는 유지하고 화면 경계에서 일부 잘렸지만 식별 가능한 사과 12개 추가 |

정답 박스는 보이는 범위에 맞췄고 가려진 크기를 추측해 확장하지 않았다.
수정 전후 그림:

- [계란 포장 20 → 43개](../runs/sale_unit_review_v2/corrected_01.png)
- [사과 20 → 32개](../runs/sale_unit_review_v2/corrected_02.png)

박스 수가 늘어난 것은 **라벨 누락을 보완한 결과**다. 이미지 수나 새 모델의 탐지 수가 늘어난 것은 아니다.

### 수정하지 않고 보류한 세 사진

| 사진 | 보류 이유 |
|---|---|
| `large green onion_001...` | 낮은 해상도에서 대파 묶음이 겹쳐 잎과 뿌리를 같은 단으로 연결하기 어려움 |
| `large green onion_017...` | 여러 묶음이 빽빽하게 겹침. 전체를 한 단으로 합치면 판매 단위 기준에 어긋남 |
| `apple_080...` | 통사과와 자른 사과가 섞여 있고 가림이 많아, 자른 부분을 별도 판매 단위로 볼지 불명확 |

보류 사진은 삭제하거나 빈 정답으로 바꾸지 않고 기존 라벨을 유지했다.
사과 `apple_006`에서도 너무 작은 배경 조각은 정체를 추측해 추가하지 않았다.
이러한 애매한 사례는 원본 고해상도·판매 맥락이 있는 사진이나 실제 카메라 촬영본으로 보완하는 편이 낫다.

### 저장 위치와 검증

- 누적 수정본: `dataset_corrected_v2/data.yaml`
- 수정본 + 새 90도 회전본: `dataset_corrected_augmented_v2/data.yaml`
- 재현 가능한 이번 수동 좌표·이유·보류 목록: [label_corrections_v2.json](label_corrections_v2.json)
- 누적 변경 전후 및 원본 해시: `dataset_corrected_v2/changes.json`

누적해서 train 라벨 5개 파일, valid 라벨 2개 파일이 원본과 다르다.
이번 추가 변경은 train 2개 파일뿐이다. train 정답은 1차 1,680개에서 **1,715개**로 35개 늘었다.
이미지는 기존과 같은 train 622장·valid 78장·test 78장이고, 증강 train은 622 + 622 = 1,244장이다.

확인한 사항:

- 원본 이미지·라벨 SHA256 일치: 원본 보존.
- v2의 valid·test 이미지와 라벨이 v1과 바이트 단위로 일치.
- train 완전 중복 행 없음, 라벨 좌표 범위 검사 통과.
- 수정 원본과 회전본 622쌍의 좌표 변환 검사 통과.
- 수정 전후 시각화에서 박스 확인.

```powershell
.\.venv-train\Scripts\python.exe prepare_corrected_dataset.py --version v2
.\.venv-train\Scripts\python.exe prepare_augmentation.py --source dataset_corrected_v2 --output dataset_corrected_augmented_v2
```

**현재 상태:** 2차 라벨 정리와 증강본 재생성 완료. 재학습은 아직 실행하지 않았다.
검증 정답이 v1과 같으므로 A 모델 비교 기준은 Precision 93.18%, Recall 56.68%, mAP50 85.75%, mAP50-95 67.44%다.
이번 작업만으로 성능이 향상됐다고 주장하지 않는다. 기존 서비스 모델·70%·1초 조건도 유지한다.
전체 데이터의 라벨 정확성을 보증하는 전수 재라벨링이 아니라, 확인한 오류를 단계적으로 보완한 결과다.

## 14. D 실험 — 수정 데이터로 동일 설정 학습, 신뢰도 60% 비교

사용자 요청에 따라 학습률을 추가 변경하지 않고 A와 같은 설정으로 학습한다.
차이는 수정 라벨을 적용한 데이터이며, 기존 A의 가중치를 이어 학습하지 않고 동일한 `yolov8n.pt`에서 시작한다.

| 항목 | 설정 |
|---|---|
| 데이터 | `dataset_corrected_augmented_v2/data.yaml` — 원본 622장 + 90도 회전본 622장 |
| 최대 에포크 / 얼리 스토핑 | 150 / 개선 없는 25에포크 |
| 배치 / 이미지 크기 | 16 / 640 |
| 옵티마이저 / 초기 학습률 | AdamW / 0.001 |
| 학습률 스케줄 / 시드 | cosine / 42 |
| 증강 | 수평 반전 확률 0.5, rotation ±15도, shear 10, 기존 A의 나머지 설정 유지 |
| GPU | RTX 4060 Laptop GPU, CUDA:0 |
| 평가 기준 | 두 모델 모두 동일한 v2 valid 78장, 신뢰도 60%, 매칭 IoU 0.5 |

60%는 학습 파라미터가 아니라 평가 시 예측을 받아들이는 기준이다.
기존 A도 같은 데이터로 다시 평가한 뒤 D와 비교한다. Precision을 우선 확인하고 Recall·F1·mAP 및 클래스별 결과를 기록한다.
기존 기록상 A의 60% 기준 정밀도 91.61%, Recall 65.44%이며 아래 표에는 이번 재평가 값을 사용한다.
완료 후 `run_corrected_experiment.py`가 이 절에 결과를 자동 기록한다.

- 학습 로그: `runs/corrected_v2_training.log`, 오류 로그: `runs/corrected_v2_training.err.log`
- 최고 가중치: `runs/detect/ozm_corrected_v2/weights/best.pt`
- 기존 A 재평가: `runs/reference_corrected_v2/report.json`
- 새 모델 평가·비교: `runs/validation_corrected_v2/report.json`, `comparison.md`
- 서비스 적용은 결과 확인 후 별도로 판단한다.

<!-- D_RESULT_START -->
### D 실험 결과 — 수정 데이터 학습 및 평가 완료

시작: 2026-09-07T21:02:25 / 완료: 2026-09-07T22:03:22
학습 136에포크, 최고 검증 에포크 111, 학습 시간 60.3분.

**동일한 v2 valid 78장, 신뢰도 60%, 매칭 IoU 0.5에서 비교.**
Precision·Recall·F1은 micro 평균. mAP는 별도로 낮은 신뢰도부터 평가.

| 지표 | 기존 A | 수정 데이터 D | 변화(%p) |
|---|---:|---:|---:|
| precision | 91.61% | 84.28% | -7.34 |
| recall | 65.44% | 61.75% | -3.69 |
| f1 | 76.34% | 71.28% | -5.07 |
| map50 | 85.75% | 85.52% | -0.23 |
| map50_95 | 67.44% | 70.45% | +3.01 |

TP/FP/FN: A 142/13/75 → D 134/25/83

| 클래스 | A 정밀도 | D 정밀도 | A Recall | D Recall |
|---|---:|---:|---:|---:|
| apple | 87.50% | 61.90% | 100.00% | 92.86% |
| bread | 100.00% | 94.12% | 88.24% | 94.12% |
| carrot | 100.00% | 100.00% | 50.00% | 50.00% |
| egg | 83.33% | 100.00% | 62.50% | 75.00% |
| galic | 100.00% | 100.00% | 83.33% | 66.67% |
| l_onion | 100.00% | 100.00% | 66.67% | 66.67% |
| onion | 97.67% | 90.48% | 72.41% | 65.52% |
| raw_pork | 82.76% | 76.67% | 40.00% | 38.33% |
| shrimp | 95.45% | 85.71% | 65.62% | 56.25% |
| sliced_ham | 62.50% | 75.00% | 83.33% | 100.00% |

60%에서 정밀도 90% 목표 충족: 아니오
최고 체크포인트는 기존 학습과 동일하게 검증 mAP50-95 기준으로 선택했다. 정밀도 최적 체크포인트라는 뜻은 아니다.
수정 valid는 v1과 같으며, test와 실제 웹 장바구니 평가는 별도다.
표본과 수정량이 작고 단일 시드 실험이므로 일반적인 성능 향상을 단정하지 않는다.
서비스 모델·임계값은 자동 변경하지 않았다.
<!-- D_RESULT_END -->

### D 결과 해석 및 적용 판단

**결론: 정밀도 우선 목표에서는 기존 A를 유지한다. D로 서비스 모델을 교체하지 않는다.**
136에포크에서 얼리 스토핑이 정상 작동했으며, 최고 체크포인트는 111에포크다.
실제 저장된 학습 설정을 비교했을 때 데이터 경로·실험명·저장 위치만 달랐다.

60%에서 D는 A보다 정탐이 8개 줄고(142 → 134), 오탐이 12개 늘었다(13 → 25).
정밀도는 7.34%p, Recall은 3.69%p 하락했다. 따라서 이번 수정 학습으로 운영 목표가 개선됐다고 볼 수 없다.

주요 클래스 변화:

- 사과: 오탐 2 → 8개, 정탐 14 → 13개. 전체 순증 오탐 12개 중 사과의 증가가 6개다.
- 양파: 오탐 1 → 4개, 정탐 42 → 38개.
- 새우: 오탐 1 → 3개, 정탐 21 → 18개.
- 고기: 오탐 5 → 7개, 정탐 24 → 23개.
- 계란: 정탐 5 → 6개, 오탐 1 → 0개로 개선됐다. 검증 정답이 8개뿐이므로 큰 일반화는 어렵다.
- 햄: 정탐 5 → 6개, 오탐 3 → 2개로 개선됐다. 검증 정답은 6개다.

**mAP50-95 상승과 정밀도 하락이 함께 나온 이유:**
mAP50-95는 여러 신뢰도 구간과 IoU 기준을 종합한다. 60% 한 지점의 정밀도·Recall과 다른 지표다.
더 엄격한 박스 일치 기준을 포함한 종합 AP는 67.44 → 70.45%로 올랐지만,
그것만으로 장바구니 오등록이 줄었다고 해석할 수 없다.
최고 가중치도 mAP50-95를 기준으로 선택했으므로 60% 정밀도가 가장 높은 체크포인트라는 뜻은 아니다.

**D의 임계값을 올리면 해결되는가?**
검사한 50~90%의 5% 간격 후보 중 D가 정밀도 90%를 처음 넘는 것은 80%다.
이때 정밀도 92.73%, Recall 47.00%다.
A는 65%에서 정밀도 93.10%, Recall 62.21%로 두 지표 모두 더 높다.
따라서 이번 후보 범위에서는 D의 임계값을 올리는 것보다 A가 유리하다.

**원인에 대해 확인된 것과 아직 모르는 것:**
수정된 데이터로 학습한 이번 D가 해당 검증 세트에서 불리했다는 것은 확인됐다.
하지만 사과의 라벨 추가가 오탐 증가의 직접 원인이라고 단정할 수는 없다.
수정량이 적고 밀집 장면에 집중됐으며, 한 번의 학습만 비교했고 검증 라벨에도 보류한 사례가 남아 있다.
오탐 증가가 실제 오탐인지 남은 정답 누락인지도 해당 예측 그림을 확인해야 구분할 수 있다.
라벨 수정을 전부 되돌리거나 추가 학습률 실험을 자동으로 시작할 근거로 사용하지 않는다.

현재 선택은 A 유지다. A의 60%는 검증상 정밀도 91.61%·Recall 65.44%인 적용 후보이며,
실제 웹의 모델·70%·1초 설정은 아직 변경하지 않았다. 이번 분석 중 추가 학습이나 서비스 변경은 하지 않았다.

## 15. 최종 추천 조건 적용 — A 모델, 신뢰도 65%·1초

사용자 요청으로 자동 장바구니 등록 기준을 70%에서 **65%**로 변경했다.
백엔드는 이미 A 모델 `runs/detect/ozm_augmented/weights/best.pt`를 사용하고 있어 그대로 사용한다.
`frontend/src/detectionTracker.js`의 `AUTO_ADD_CONFIDENCE = 0.65`를 판정과 화면 안내가 함께 참조한다.
1초 지속 조건 및 화면에서 사라진 뒤 재등장하면 다시 등록하는 동작은 유지한다.

동일한 수정 valid에서 A의 65% 결과는 정밀도 93.10%, Recall 62.21%다.
70%의 정밀도 93.18%와 거의 같으면서 Recall 56.68%보다 높아 선택했다.
이 수치는 정적 검증 이미지 평가이며 실제 카메라 등록 성공률은 아니다.

백엔드의 `conf=0.25`는 낮은 신뢰도에서도 상품이 계속 보이는지 추적하기 위한 추론 출력 기준이고,
`iou=0.7`은 겹치는 박스 처리 기준이다. 자동 등록 신뢰도와 달라서 이 값들은 변경하지 않는다.
65% 경계·1초 지속·낮은 신뢰도 시 초기화·사라진 뒤 재등록을 검사하는 기존 테스트 3개가 통과했다.
